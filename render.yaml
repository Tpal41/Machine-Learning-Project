services:
  - type: web
    name: college-placement-predictor
    env: python
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app_enhanced:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
    envVars:
      - key: MPLBACKEND
        value: Agg
